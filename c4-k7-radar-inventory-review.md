# C4 K7 프리미어 레이더 읽기 전용 진단 검토 자료

## 목적

K7 프리미어의 전방 레이더가 다중 레이더 트랙을 지원하는 모델인지 판단하기 전에 ECU 정보를 안전하게 확인한다.

이 단계에서는 레이더 설정을 변경하거나 트랙 기능을 활성화하지 않는다. ECU 부품번호, 펌웨어 정보, 현재 DID `0x0142` 값만 읽는다.

## 대상 저장소와 브랜치

- GitHub 저장소. `shake777/openpilot`
- 대상 브랜치. `carrot-wip-custom`
- C4에 설치된 기능 커밋. `5e52e89a`
- 기록 문서 커밋. `7f7c8d17`

공통 변경은 다음 유지 브랜치에도 반영했다.

- `carrot-wip`
- `carrot-wip-custom`
- `carrot-wip-web-chg-branch-filter`

## 구현 위치

- 진단 스크립트. `openpilot/selfdrive/debug/car/hyundai_enable_radar_points.py`
- 단위 테스트. `openpilot/selfdrive/debug/car/test_hyundai_radar_inventory.py`

## 추가된 실행 모드

`--inventory-only` 옵션을 추가했다.

이 모드에서 허용되는 UDS 요청은 다음 두 개뿐이다.

1. DID `0xF100` 읽기.
2. DID `0x0142` 읽기.

출력에는 다음 정보가 포함된다.

- ECU 송신 주소 `0x7D0`.
- ECU 수신 주소 `0x7D8`.
- 사용한 CAN 버스.
- `0xF100` 원시 응답의 16진수 값.
- 응답에서 추출한 ECU 부품번호와 펌웨어 버전.
- `0x0142` 원시 응답의 16진수 값과 길이.
- 기존에 알려진 활성화 대상 펌웨어와의 정확한 일치 여부.
- 쓰기 수행 여부 `false`.
- 진단 세션 변경 여부 `false`.

## 안전 제한

`--inventory-only` 실행 시 다음 동작은 하지 않는다.

- ECU DID 쓰기.
- 확장 진단 세션 또는 다른 진단 세션으로 변경.
- DID 범위 스캔.
- 레이더 트랙 활성화.
- SCC 또는 AEB 제어 변경.
- `AlphaLongitudinalEnabled` 설정 변경.

또한 `--inventory-only`는 `--read-only`, `--scan-config-dids`, `--default` 옵션과 동시에 사용할 수 없게 했다.

## 검증 결과

다음 검증을 통과했다.

- `python -m unittest -v openpilot.selfdrive.debug.car.test_hyundai_radar_inventory`
- Python 문법 컴파일 검사.
- `git diff --check`.

단위 테스트에서는 진단 클라이언트에 전달되는 요청이 정확히 `0xF100`, `0x0142` 두 개뿐인지 확인한다. 예제 응답에서는 부품번호 `99110-F6000`, 버전 `1.00`, `1.02`, 설정값 `000000010000`을 추출하는 것도 확인한다.

## 실제 C4 적용 결과

C4는 `shake777/openpilot`의 `carrot-wip-custom` 기능 커밋 `5e52e89a`로 업데이트했다.

실제 장비에서 다음 상태를 확인한 후 읽기를 시도했다.

- 차량 상태 파라미터 `IsOnroad=0`.
- 차량 상태 파라미터 `IsOffroad=1`.
- 진단 중 기존 `comma` 프로세스를 정지.
- 진단 종료 후 `comma`와 `pandad`를 다시 실행.

정상적인 C4 Python 환경으로 `--inventory-only`를 실행했고 Panda 장치 연결에는 성공했다. 그러나 첫 번째 DID `0xF100` 요청에서 응답 시간 초과가 발생했다.

따라서 실제 차량에서는 아직 다음 값을 얻지 못했다.

- 레이더 ECU 부품번호.
- 레이더 ECU 펌웨어 버전.
- DID `0x0142` 현재 값.

`0xF100`에서 중단됐기 때문에 `0x0142` 요청은 전송되지 않았다. 진단 세션 변경이나 ECU 쓰기도 실행되지 않았다.

## 현재 판단

응답 시간 초과만으로 레이더가 다중 트랙을 지원하지 않는다고 판단할 수 없다. 다음 가능성을 먼저 확인해야 한다.

- 차량 전원이 완전한 IGNITION ON 상태가 아니었을 가능성.
- 레이더 ECU의 진단 통신 경로가 CAN 버스 0이 아닐 가능성.
- 게이트웨이가 현재 상태에서 진단 응답을 전달하지 않았을 가능성.

## 다음 안전 확인 절차

1. 차량을 안전한 장소에 정차한다.
2. 브레이크를 밟지 않고 START 버튼을 두 번 눌러 계기판 전체가 켜진 IGNITION ON 상태로 만든다.
3. 엔진과 구동 시스템은 작동시키지 않는다.
4. C4를 인터넷에 연결한다.
5. 먼저 CAN 버스 0에서 `--inventory-only`를 다시 실행한다.
6. 다시 시간 초과가 발생하면 임의의 DID 스캔이나 쓰기를 하지 않는다.
7. 기존 CAN 로그와 차량 인터페이스 설정을 근거로 올바른 진단 버스를 식별한 후 동일한 두 DID만 읽는다.
8. 부품번호와 펌웨어가 확인된 뒤에만 해당 레이더 모델의 다중 트랙 지원 여부를 별도로 검토한다.

## 검토 요청

다른 검토자는 다음 사항을 확인해 주기 바란다.

- `--inventory-only` 경로에서 UDS 쓰기 함수가 호출될 가능성이 없는지.
- 진단 세션 변경 함수가 `--inventory-only`보다 먼저 호출되지 않는지.
- 실제 요청 DID가 `0xF100`, `0x0142`로 제한되는지.
- 예외가 발생해도 `comma`와 `pandad`가 정상 복구되는지.
- K7 프리미어에서 레이더 진단 주소 `0x7D0/0x7D8`과 CAN 버스 0이 정확한지.
- 부품번호와 펌웨어가 확보되기 전에 활성화 값을 쓰지 않는지.

## 검토 후 수정 사항.

- F100 및 0142의 시간 초과와 거부 응답을 DID별 JSON 결과에 기록한다. F100 성공 후 0142가 실패해도 부품번호와 원시 응답을 보존한다.
- F100부터 실패하면 0142는 not_read로 남기고 추가 요청 없이 종료한다. 완전한 성공은 종료 코드 0, 부분 결과 또는 조회 실패는 2다.
- known_activation_support는 unknown으로 표시한다. firmware_exact_match는 별도 목록 일치 정보이며, 일치만으로 실제 차량에서 활성화 가능하다고 판정하지 않는다.
- 기존 활성화 경로의 기본 설정값 검사를 ECU 쓰기 앞으로 옮겼다. 모의 장치에서 예상하지 않은 설정값이면 쓰기가 한 번도 실행되지 않는지 검증한다.
- 인벤토리 종료 시 Panda 연결을 닫는다. 이 동작이 Panda 안전 모드 복원이나 comma/pandad 재시작을 보장하지는 않는다. 서비스 복구는 실행 운영 절차에서 별도로 확인해야 한다.
- 위 단위 테스트 예제는 합성값이며 K7 실측값이 아니다. 실패 및 CLI 경로를 포함한 테스트 6개를 통과했다.

## 외부 검토 보고서의 정정 사항.

- IsOffroad=1만으로 물리적 점화 OFF 또는 응답 시간 초과 원인을 확정할 수 없다.
- 기본 세션과 세션 0x07의 응답 차이로 목록 불일치가 생길 수 있으나, 항상 false라고 단정할 수는 없다.
- 버스 0은 재확인할 후보이며 실제 레이더 응답으로 경로가 검증되지는 않았다. 다른 ECU 응답으로 레이더 통신 성공까지 입증할 수 없다.
- 트랙 개수와 물리적 케이블 요구사항은 차량, DBC 및 코드 버전을 기준으로 확인해야 한다.
- EnableRadarTracks=0은 자동 트랙 활성화 경로를 막지만 종방향 제어 설정에 따른 별도 ECU 통신 비활성화 경로까지 막지는 않는다. SCC 유지 여부는 별도 확인해야 한다.
- 앞의 C4 설치 커밋과 실차 시간 초과 기록은 이전 시험 시점의 기록이다. 이번 보완의 실차 적용 여부와 동일시하지 않는다.

## 중요 결론

현재 구현은 정보 확인을 위한 읽기 전용 단계다. 실제 레이더 트랙 활성화 여부는 아직 결정되지 않았다. 차량별 ECU 식별 결과와 로그 재생 검증 없이 레이더 설정 변경이나 종방향 제어 연결을 진행해서는 안 된다.
