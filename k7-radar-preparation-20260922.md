# K7 레이더 검토 결과와 차량 실행 안내

## 결론

첨부한 인벤토리 검토, 프로브 검토, 0~2단계 절차서와 `radar_track_verdict.py`를 현재 `carrot-wip-custom` 코드에 대조했다. 문서 전체를 그대로 적용하지 않고, 아래 오류를 보정해 오프라인 분석과 **설정·세션·보안 변경 없는 수동 정차 수집**을 준비했다. 레이더 트랙 활성화에 성공했다는 뜻은 아니다.

사용자 차량에는 다른 사용자 차량의 물리 SCC 토글이 없다. 사용자가 켠 `AlphaLongitudinalEnabled`, `EnableRadarTracks`, `HyundaiCameraSCC`, 차종 선택과 기존 종방향 제어는 변경하지 않았다.

## 첨부 내용에서 보정한 사항

| 첨부 제안·단정 | 현재 코드 및 증거에 따른 판단 |
|---|---|
| `STATE != 0`이면 유효 타깃이다. | 현재 Mando 경로의 유효 상태는 `3`, `4`다. 분석도 동일하게 맞췄다. |
| 트랙은 반드시 첫 32개 주소뿐이다. | 현재 DBC와 파서는 상위 32개도 선택적으로 처리한다. 두 영역을 구분해 보존한다. |
| `0x501/0x502`를 항상 타깃 수에서 제외한다. | 현재 파서에는 이 제외 규칙이 없다. 임의 제외하지 않는다. |
| 같은 거리의 타깃은 하나로 센다. | 서로 다른 슬롯은 거리가 같아도 별도로 센다. 슬롯 수가 실제 차량 수라는 보장은 없다. |
| 짧은 창 5개에서 두 점이면 `multi_target_confirmed`다. | DBC에 맞는 후보 관측으로만 표시한다. 실물 비교 전에는 활성화·물리 타깃을 확정하지 않는다. |
| 관측 0이면 레이더가 트랙을 지원하지 않는다. | 해당 캡처의 버스·주소 범위에서 미관측일 뿐이다. 배선·설정·캡처 범위 등은 별도 문제다. |
| F186의 NRC 0x31이면 확장 세션 및 보안 접근 전체가 실패했다. | 해당 DID 읽기 거절이다. 이전 `0x03` 긍정응답 및 `0x0142` 읽기 성공과 구분한다. |
| 복귀 실패 경로·오류 누적을 새로 구현해야 한다. | 세션 시도 전 플래그, 복귀 실패 후 설정 재읽기, 단계별 오류 배열은 이미 구현돼 있다. |
| 이웃 DID 길이가 같으면 동일 설정 블록이다. | 구조를 추정할 단서일 뿐이며 의미·활성화 비트를 입증하지 않는다. |
| 검색 결과가 없으므로 세계 최초 사례다. | 첨부 조사 범위에서 사례를 확보하지 못했다는 의미로 한정한다. |

기존 지원 레이더의 6바이트 설정을 K7의 5바이트 설정에 그대로 적용할 근거는 없다. 이전 `0002000001` 쓰기 거절은 확인된 실패이며, 이번 도구는 이를 재시도하지 않는다.

## 이번에 준비한 기능

1. `tools/c4_diagnostics/analyze_radar_tracks.py`에 Mando 필드 디코드, 상태별 수, 거리·상대속도 범위, 최근 50 ms 동시 슬롯 통계를 추가했다. 잘못된 버스·송신 에코·DLC는 제외한다. 결과는 후보 판정이며 제어에 연결하지 않는다.
2. `tools/c4_diagnostics/characterize_radar.py`는 펌웨어와 기존 `0142` 값이 일치할 때만 제한된 읽기를 진행한다. F186, F181, F187~F18B, 0140~0148, 전후 DTC와 최종 0142를 기록한다. VIN·ECU 시리얼은 요청하지 않는다.
3. 이번 수집에는 확장 세션 진입, 보안 시드·키, DID 쓰기, DTC 삭제, ECU 리셋이 없다. F186이 지원되지 않아도 오류를 기록한 뒤 다른 읽기를 진행한다. 이번에는 보안 요청을 하지 않으므로 세션 인증 우회가 아니다.
4. `parked_probe.py --characterize`로 기존 격리된 일회성 실행·서비스 재시작·업로드 대기열을 사용한다. 재시작 성공만으로 진단 실패를 성공 처리하던 종료 코드를 바로잡았다.

각 읽기는 최소 50 ms 간격으로 한 번만 요청한다. UDS 응답 대기 설정과 별개로 작업 전체에는 기존 120초 제한이 있다. 제한에 걸리거나 결과 파일이 없으면 확인 실패로 남긴다. 설정 쓰기가 없더라도 진단 연결과 openpilot 중지·재시작은 있으므로 주행 중 실행하면 안 된다.

## 차량에서 한 번 실행하는 방법

배포된 변경을 업데이트한 뒤 실행한다. 업데이트 실패 시 강제 초기화나 파일 삭제로 진행하지 않는다.

1. 안전하게 주차하고 P단·주차브레이크를 적용한다. 엔진은 끄고 계기판이 켜진 점화 ON 상태로 둔다. ACC만 켠 상태와 구분한다.
2. 휴대폰에서 C4의 기존 `6999` 복구 터미널을 연다.
3. 아래를 실행하고 확인 문구에 대문자 `PARKED`를 입력한다.

```sh
cd /data/openpilot
python3 tools/c4_diagnostics/parked_probe.py --characterize
```

4. 단순 업데이트·재부팅만으로 이 새 진단은 실행되지 않는다. 실행 중 차량 시동·주행·추가 진단 명령을 하지 않는다. 터미널 연결이 끊길 수 있다.
5. 실행 종료 후 연결을 다시 열고 다음 결과를 확인한다.

```sh
cat /data/c4-diagnostics/k7-parked-probe-status.json
```

확인할 항목은 `mode="characterize"`, `probe_started=true`, `final_config_verified=true`, `restore_status="not_needed"`, `comma_restarted=true`다. `not_needed`는 세션 변경 자체를 하지 않았다는 뜻이지, 차량 전체의 정상 보증이 아니다.

- `probe_returncode=0`은 수집과 설정 불변 확인 완료다.
- `2`는 일부 읽기 실패이며 보고서의 단계별 오류를 분석해야 한다. F186 미지원도 여기에 해당할 수 있다.
- `3`, `5`, `124`, `error` 발생이나 최종 설정 확인 실패는 정상 완료가 아니다.
- `dtc_changed=null`은 DTC 비교 불가이며 무고장이라는 뜻이 아니다. `true`면 전후 내용 검토가 필요하다.

보고서는 기존 `k7-security-probe-*.json` 업로드 대기열에 저장되지만 `mode`로 새 무보안 진단을 식별한다. 인터넷 연결 및 업로더의 정상 동작이 필요하며, 파일 생성만으로 서버 수신을 확정하지 않는다. 새 차량 경고나 복구 이상이 있으면 이번 결과만으로 운행·보조 기능 사용 가능 여부를 판단하지 않는다.

## 다음 판단

새 보고서에서는 부품·소프트웨어 식별값, 인접 DID 지원 여부와 길이, DTC 전후 차이를 먼저 비교한다. 이 자료만으로 숨은 활성화 명령이나 키가 자동으로 도출되지는 않는다. 동일 부품·펌웨어의 실제 활성화 통신이나 설정 의미를 입증하는 자료가 확보돼야 쓰기 시험을 별도로 검토할 수 있다.

## 검증 범위

로컬 단위·회귀 테스트 92개와 Python 컴파일 검사가 통과했다. CLI에서 새 모드 선택, 쓰기 모드 동시 선택 거부, 보고서 저장, Panda 닫기, 실패 후 서비스 재시작도 가짜 클라이언트로 검증했다. `git diff --check`도 통과했다.

별도 `test_inventory_transport` 통합 테스트는 Windows의 Cap'n Proto 스키마 로딩 중 프로세스가 중단돼 실행하지 못했다. 이를 통과로 계산하지 않았다. 이번 작업에서 실차 요청이나 서버 업로드는 실행하지 않았다. 이전 분석에 사용된 임시 캡처 파일은 현재 경로에 없어 새 분석기로 재검증하지 못했다. 따라서 이전 캡처의 수치를 이번 실행 결과로 재사용하지 않는다.

주행 레이더 처리·선행차 선택 코드는 변경하지 않았으므로 NAS 레이더 재생 엔진 배포 대상 변경은 없다.
