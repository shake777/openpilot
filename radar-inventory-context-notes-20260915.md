# K7 프리미어 레이더 읽기 전용 인벤토리 컨텍스트 노트

- 현재 `--read-only`는 DID 쓰기를 하지 않지만 0142 기본 세션 읽기 실패 시 확장 진단 세션으로 전환한다.
- `KIA_K7_PE`는 차량 플랫폼에 등록되어 있지만 전방 레이더 펌웨어 목록과 활성화 지원 목록에는 K7 항목이 없다.
- 인벤토리 결과가 기존 활성화 지원 목록과 다르면 미지원으로 단정하지 않고 `unknown`으로 유지한다.
- 트랙 지원 최종 판정은 인벤토리와 별도로 SCC11 및 0x500 계열 CAN 메시지의 수동 관찰이 필요하다.
- `--inventory-only`는 기본 세션에서 F100과 0142만 읽는 조기 종료 경로이며 확장 세션, 전체 DID 스캔, 쓰기·복구 옵션과 조합할 수 없다.
- 출력에는 ECU 주소와 버스, F100 원본 HEX·ASCII·부품번호·버전 필드, 0142 원본 HEX·길이, 기존 지원 목록 일치 여부가 포함된다.
- 표준 `unittest`와 Python 컴파일, `git diff --check`가 통과했다.
- 커밋 `5e52e89a`를 C4의 `carrot-wip-custom`에 fast-forward로 적용했다.
- C4는 `IsOnroad=0`, `IsOffroad=1`에서 comma tmux를 중지한 뒤 `/usr/local/venv/bin/python3`로 버스 0 인벤토리를 실행했다.
- 레이더 ECU가 첫 요청인 F100에 응답하지 않아 `MessageTimeoutError`로 종료됐다. 0142, 확장 진단 세션, DID 스캔, 쓰기 서비스는 실행되지 않았다.
- 종료 처리 후 comma tmux와 `pandad`가 모두 다시 실행 중임을 확인했다. 점화 ON 상태 확인 후 버스 경로를 확정해 재시도해야 한다.

## 검토 후 보완 결정.

- 정상 응답만 검증했던 기존 테스트로는 부분 결과 유실과 시간 초과를 발견하지 못했다. DID별 실패 및 실제 CLI 분기를 모의 장치로 추가 검증한다.
- IsOffroad는 openpilot 실행 상태로, 물리적 점화 OFF와 시간 초과 원인을 단독으로 확정하는 증거가 아니다.
- 기존 세션 0x07 펌웨어 목록과 기본 세션 응답의 불일치로 레이더 미지원을 판정하지 않는다. 부품번호만으로 활성화를 허용하지 않는다.
- 이 보완은 수동 진단 도구에 한정된다. ECU 시험, 트랙 활성화, 차량 제어 설정 변경은 이번 작업에 포함하지 않는다.
- DID 오류 시 status/error_type/message 및 거부 코드 NRC를 보존한다. F100 오류 시 0142는 not_read로 남기고 추가 요청을 중단한다. 부분 결과의 CLI 종료 코드는 2다.
- known_activation_support는 unknown으로 유지하고 firmware_exact_match로 원문 일치 여부를 따로 제공한다. 기존 boolean 필드의 소비자는 새 문자열 상태를 명시적으로 비교해야 한다.
- 인벤토리 경로의 finally에서 Panda 연결을 닫는다. 이는 ELM327 안전 모드 복원이나 comma/pandad 서비스 재시작을 대신하지 않으며, 서비스 복구는 실행 운영 절차가 담당한다.
- 정상 조회, 두 DID의 시간 초과 및 거부 응답, 목록 일치, CLI 종료, 쓰기 전 기본값 검사, 기존 정상 활성화·복원 경로를 모의 장치로 검증했다. 실차 지원 여부는 아직 미확정이다.
