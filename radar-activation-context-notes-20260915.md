# 레이더 활성화 응답 보완 결정 기록.

- 기존 자동 활성화 함수는 2E 요청에 68 응답을 기대하고 get_data(0) 결과가 비어 있어도 True를 반환했다. 독립 모의 시험으로 재현했다.
- 정상 쓰기 응답은 저장소 UDS 처리 기준으로 6E이며 DID 0142까지 일치해야 한다. 성공은 쓰기 확인 의미이고 다중 트랙 수신·SCC/AEB 정상 동작까지 뜻하지 않는다.
- 대상 버스 선택과 기존 세션·쓰기 내용은 유지한다. 재전송, 지원 펌웨어 확대, 추가 ECU 진단 명령은 넣지 않는다.
- EnableRadarTracks 및 AlphaLongitudinalEnabled의 저장값을 변경하지 않는다. EnableRadarTracksResult는 코드 검색상 내부 결과 파라미터로 확인했다.
- C4 캡처는 기존 C4RADAR1 형식을 유지한다. 진단 원문은 CAN 및 sendcan 구독으로만 관찰하며 캡처 서비스는 CAN을 송신하지 않는다.
- 시작 직전 응답을 놓치지 않도록 장면 캡처 시작 전 진단 프레임을 최대 256개 보관한다. 서비스 시작 전 데이터나 별도 실행된 진단 도구의 결과까지 보장하지는 않는다.
- 레이더 감지·선행차 선택·재생 알고리즘 변경이 아니므로 NAS 레이더 재생 이미지 배포 대상이 아니다. 원본 진단 CAN은 기존 이진 디코더로 읽을 수 있다.
- 원격 브랜치를 확인하니 C4 업로더는 custom 브랜치에만 있다. 공통 활성화 수정만 모든 유지 브랜치에 반영하고 C4 전용 코드는 다른 브랜치로 전파하지 않는다.
- 세션과 쓰기는 각각 기본 대기 0.5초, 전체 1초로 제한한다. 기존 세션과 쓰기 요청 외에 새 ECU 요청이나 재시도는 하지 않는다.
- common 활성화 시험 8개 및 custom 캡처·업로드·인벤토리 시험 22개가 통과했다. Python 문법 검사와 git diff --check도 통과했다.
- 실제 ECU나 C4에 접속하지 않은 모의 시험이다. 차량에서 트랙을 켜도 된다는 판정은 하지 않는다.
